<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_logo"

    android:gravity="center"
    tools:context=".ui.splash.NoNetworkActivity">

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:tint="@color/colorAccent"
        android:contentDescription="No internet connection"
        android:src="@drawable/ic_network_check_black_24dp"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="22sp"
        android:layout_marginTop="8dp"
        android:textColor="@color/colorPrimary"
        android:text="No Internet Connection"
        android:fontFamily="@font/roboto_medium"/>
    <TextView
        android:layout_width="400dp"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textAlignment="center"
        android:layout_marginTop="4dp"
        android:gravity="center_horizontal"
        android:text="You need internet connection in other to set up NiCare Mobile EES on your device, please connect to a WIFI or mobile network to proceed"
        android:fontFamily="@font/roboto_regular"/>

    <Button
        android:id="@+id/btnTryAgain"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:background="@drawable/border_button_grey"
        android:textAllCaps="false"
        android:layout_marginTop="32dp"
        android:textColor="@color/white"
        android:text="Try Again"/>

</LinearLayout>